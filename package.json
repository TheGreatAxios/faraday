{
  "name": "faraday",
  "version": "0.1.0",
  "private": true,
  "description": "An agent-native reading room for volumetric medical imaging. The agent reads the scan; the scan never leaves the tab.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "record-demo": "bun run scripts/record-demo.ts"
  },
  "dependencies": {
    "@niivue/niivue": "next",
    "@thegreataxios/webmcp-core": "file:./vendor/webmcp-core",
    "@thegreataxios/webmcp-react": "file:./vendor/webmcp-react",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "@webgpu/types": "^0.1.72",
    "playwright": "^1.62.1",
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
